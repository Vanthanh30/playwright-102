export interface SimpleMessageData {
  message: string;
}

export interface SliderData {
  initialValue: number;
  targetValue: number;
}

export interface ContactFormData {
  name: string;
  email: string;
  password: string;
  company: string;
  website: string;
  country: string;
  city: string;
  address1: string;
  address2: string;
  state: string;
  zipCode: string;
}
